// websocket/ — Reserved for real-time updates via WebSocket.
// Intended use: push live notifications when books are borrowed/returned,
// and broadcast dashboard stats updates without polling.
//
// Suggested library: socket.io
// Example: npm install socket.io

export {};
