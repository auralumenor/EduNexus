PORT=5000
NODE_ENV=development
MONGO_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/edunexus?retryWrites=true&w=majority

# JWT Signature Key (Change this before deploying to production!)
JWT_SECRET=lms_super_secret_key_change_in_production
JWT_EXPIRES_IN=7d

# Email Configuration (Leave empty for Ethereal Local testing)
# For production or SMTP-based providers:
SMTP_HOST=
SMTP_PORT=
SMTP_EMAIL=
SMTP_PASSWORD=
FROM_NAME="EduNexus"
FROM_EMAIL="noreply@yourdomain.com"
