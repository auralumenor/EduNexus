// Lightweight global store using React Context + useReducer.
// No Redux/Zustand dependency required for current scope.
// Add slices here as the app grows (e.g. booksSlice, uiSlice).

export {}; // Reserved for future state management expansion
